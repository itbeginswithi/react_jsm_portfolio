import React, {useState} from 'react'
import { motion } from 'framer-motion';
import ReactToolTip ffrom 'react-tooltip';

import {AppWrapper} from '../../wrapper'
import {images} from '../../constants'
import './Skills.scss'

const Skills = () => {
  return (
    <div>Skills</div>
  )
}

export default Skills