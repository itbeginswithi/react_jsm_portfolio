import React from 'react'

import { NavigationDots, socialMedia } from '../components';

const AppWrapper = (Component, idName, class) => {
  return (
    <div>
        
    </div>
  )
}

export default AppWrapper